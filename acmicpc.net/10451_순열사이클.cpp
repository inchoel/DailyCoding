#include <cstdio>
#include <algorithm>
#include <utility>
using namespace std;

int main() {
  int t, n;
  scanf (" %d", &t);
  while (t--) {
    scanf (" %d", &n);
    vector<int> v(n);
    for (int i=0; i<n; i++) {
      scanf (" %d", &v[i]);
    }

  }

  return 0;
}
