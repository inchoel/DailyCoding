#include <cstdio>
#include <vector>
using namespace std;

int main() {
  unsigned long int a, b;
  scanf (" %d %d", &a, &b);

  int m;
  scanf (" %d", &m);

  int posNum[26];
  for (int i=0; i<m; i++) {
    scanf (" %d", &posNum[i]);
  }


  return 0;
}
